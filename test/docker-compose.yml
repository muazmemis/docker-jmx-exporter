version: '3.2'

services:
  exporter:
    image: muazmemis/jmx-exporter
    ports:
      - 5556
  query:
    image: muazmemis/jmx-exporter
    entrypoint:
      - curl
    command:
      - -s
      - "http://exporter:5556/metrics"
